export interface IItemsIds {
  result: string[];
}

export interface IItemsData {
  result: [];
};

export type ItemType = {
  brand: string | null;
  id: string;
  price: number;
  product: string;
};